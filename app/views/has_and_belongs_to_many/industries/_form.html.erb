<%= form_with(model: industry, url: {action: action}) do |form| %>
  <% if industry.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(industry.errors.count, "error") %> prohibited this industry from being saved:</h2>

      <ul>
        <% industry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.text_field :email %>
  </div>
  <div>
    <%= form.label :sectors, style: "display: block" %>
    
    <% getSectors.each do |sector|%>
      <%= form.check_box :sector_ids, {multiple: true}, sector[1], false%>
      <%= form.label sector[0]%>
    <% end%>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
