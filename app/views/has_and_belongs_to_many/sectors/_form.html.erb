<%= form_with(model: sector, url: {action: action}) do |form| %>
  <% if sector.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(sector.errors.count, "error") %> prohibited this sector from being saved:</h2>

      <ul>
        <% sector.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <% getIndustries.each do |industry|%>
      <%= form.check_box :industry_ids, {multiple: true}, industry[1], false%>
      <%= form.label industry[0]%>
    <% end%>
  <div>
    <%= form.submit %>
  </div>
<% end %>
